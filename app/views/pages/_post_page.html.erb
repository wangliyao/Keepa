<table class="table table-hover">
	<tbody>
	  <thead>
	  	<tr>
	  		<th>id</th>
	  		<th>标题</th>
	  		<th>文摘</th>
	  	</tr>
	  </thead>
	<% if @posts.present? %>
	  <% @posts.each do |t| %>
      <tr>
    	<td><%= t.id %></td>
    	<td><%= t.title %></td>
    	<td><%= t.abstract%></td>
      </tr>
      <% end %>
    <% end %>
	</tbody>
</table>